import styled from 'styled-components'
import { theme } from '../../styles/theme'

export const MovieSectionStyled = styled.h2`
    color: ${theme.colors.grey};
    position: relative;
    border-bottom: 1px solid ${theme.colors.grey};
    padding-top: 1.5rem;
    margin-bottom: 1rem;
    z-index: 2;
`